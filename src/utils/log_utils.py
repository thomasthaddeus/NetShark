"""log_utils.py
5. **`log(message, log_type="INFO")`**:
    - Purpose: Log messages to a file or console.
    - Parameters:
        - `message`: The message to log.
        - `log_type`: The type of log message (e.g., "INFO", "ERROR", "DEBUG").

"""

from logging import Logger

def log(message, log_type="INFO"):
    pass
